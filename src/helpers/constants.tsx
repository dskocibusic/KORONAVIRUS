import { Dimensions } from "react-native";

export const { width, height } = Dimensions.get("window");

export const ICON_SIZE: number = 25;
export const TEXT_INPUT_WIDTH: number = width * 0.9;
export const TEXT_INPUT_HEIGHT: number = 50;

export const BUBBLE_WIDTH: number = width / 2.2;
export const BUBBLE_HEIGHT: number = 50;


export const DETAILS_CONTAINER_WIDTH: number = width * 0.9;
export const DETAILS_CONTAINER_HEIGHT: number = 80;
export const IMAGE_SIZE: number = 100;

export const MENU_WIDTH: number = 100;
export const MENU_HEIGHT: number = 40;